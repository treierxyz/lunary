---

---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <!-- Favicon from Twemoji, Copyright 2020 Twitter, Inc and other contributors, CC-BY 4.0: https://creativecommons.org/licenses/by/4.0/ -->
    <link rel="shortcut icon" type="image/x-icon" href="/cheese.png" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>❓</title>
    <script src="../scripts/moon.js"></script>
  </head>
  <body>
    <div id="toast">
      <p>Emoji copied to clipboard</p>
    </div>
    <div class="container">
      <h1 class="title">Current Moon phase is:</h1>
      <div id="emoji">❓</div>
      <p id="phase">Unknown</p>
      <div class="layered">
        <div class="progressbar">
          <div class="bg">
            <!-- <div id="bar"></div> -->
          </div>
          <p class="percentage">--.----%</p>
        </div>
        <div class="progressbar fullprogress">
          <div class="bg">
            <!-- <div id="bar"></div> -->
          </div>
          <p class="percentage">--.----%</p>
        </div>
      </div>
      <div id="progress-marks">
        <p>🌑</p>
        <p>🌓</p>
        <p>🌕</p>
        <p>🌗</p>
        <p>🌑</p>
      </div>
    </div>
    <footer>
      <p>Made by <a href="https://treier.xyz">treierxyz</a></p>
    </footer>
  </body>
</html>

<style>
  /* @link https://utopia.fyi/type/calculator?c=320,18,1.2,1240,20,1.25,5,2,&s=0.75|0.5|0.25,1.5|2|3|4|6,s-l&g=s,l,xl,12 */

  :root {
    --step--2: clamp(0.78rem, calc(0.77rem + 0.03vw), 0.8rem);
    --step--1: clamp(0.94rem, calc(0.92rem + 0.11vw), 1rem);
    --step-0: clamp(1.13rem, calc(1.08rem + 0.22vw), 1.25rem);
    --step-1: clamp(1.35rem, calc(1.28rem + 0.37vw), 1.56rem);
    --step-2: clamp(1.62rem, calc(1.5rem + 0.58vw), 1.95rem);
    --step-3: clamp(1.94rem, calc(1.77rem + 0.87vw), 2.44rem);
    --step-4: clamp(2.33rem, calc(2.08rem + 1.25vw), 3.05rem);
    --step-5: clamp(2.8rem, calc(2.45rem + 1.77vw), 3.82rem);

    --primary: #55aec7;
    --secondary: #fff2a9;
    --background: #1d1f20;

    --progress-width: 0%;

    color-scheme: only light;
  }

  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  * {
    margin: 0;
    padding: 0;
    font: inherit;
    font-family: sans-serif;
    text-align: center;
    color: var(--secondary);
    text-decoration: none;
  }

  a:hover {
    color: var(--primary);
  }

  body {
    background-color: var(--background);
    min-height: 100svh;
  }

  h1 {
    font-size: var(--step-3);
    font-weight: 600;
    text-wrap: balance;
  }

  p {
    font-size: var(--step--1);
  }

  .container {
    display: flex;
    height: 100svh;
    flex-direction: column;
    justify-content: center;
    width: min(100% - 3rem, 65ch);
    margin-inline: auto;
  }

  .container > * + * {
    margin-top: 1rem;
  }

  #emoji {
    font-size: clamp(10rem, calc(8.45rem + 10vw), 50rem);
    user-select: none;
  }

  #phase {
    font-size: var(--step-0);
  }

  .progressbar {
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    border-radius: 10px;
  }

  .progressbar > .bg {
    position: relative;
    width: 100%;
    height: 2rem;
    vertical-align: middle;
    background-color: var(--secondary);
  }

  .percentage {
    position: absolute;
    color: var(--background);
    /* mix-blend-mode: luminosity; */
  }

  .layered {
    display: inline-grid;
  }

  .layered > * {
    grid-column-start: 1;
    grid-row-start: 1;
  }

  .progressbar.fullprogress {
    clip-path: xywh(0 0 var(--progress-width) 100%);
  }

  .progressbar.fullprogress > .bg {
    background-color: var(--primary);
  }

  .fullprogress > .percentage {
    position: absolute;
    color: var(--secondary);
    /* mix-blend-mode: luminosity; */
  }

  #progress-marks {
    display: flex;
    justify-content: space-between;
    margin-top: 0;
  }

  footer {
    position: fixed;
    bottom: 1rem;
    left: 0;
    right: 0;
  }

  #toast {
    position: fixed;
    background-color: var(--primary);
    border-radius: 6px;
    transform: translateX(-50%);
    left: 50%;
    bottom: 100%;
  }

  #toast p {
    color: var(--secondary);
    padding: 0.5rem 1rem;
  }

  .toastAnimation {
    animation: toastMove 3s ease;
  }

  @keyframes toastMove {
    0% {
      transform: translate(-50%, -100%);
    }

    20% {
      transform: translate(-50%, calc(100% + 1rem));
    }

    80% {
      transform: translate(-50%, calc(100% + 1rem));
    }

    100% {
      transform: translate(-50%, -100%);
    }
  }
</style>
